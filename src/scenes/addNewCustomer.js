import React from 'react';
import {SafeAreaView, Text, TouchableHighlight} from 'react-native';

const NewCustomer = ({navigation}) => {
  <SafeAreaView>
    <Text>This is the Add New Customer screen</Text>
  </SafeAreaView>;
};

export default NewCustomer;
